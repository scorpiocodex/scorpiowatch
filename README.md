# WatchFlow

> React to anything. Orchestrate everything. From your laptop, to your CI pipeline, to your AI agents.

A cross-platform, event-driven workflow orchestration engine for **local
development**, **CI/CD**, **DevOps automation**, and **MCP-powered AI systems** —
async-native top to bottom, safe by default, and small enough to read in an
afternoon.

WatchFlow sits between *"something happened"* and *"the right work ran, safely,
with a record of it"*: a file save, a cron tick, a webhook, a queue message, a git
push, or an AI agent's MCP tool call all flow through one async pipeline —
normalized to an Event, matched to a Trigger, scheduled with dedupe, and executed
as a Workflow of `shell=False` steps with a durable audit trail.

## Status

Pre-release planning (`v0.1.0` bootstrap). No public stability guarantees yet.

## Documentation

The full documentation set lives in [`docs/`](./docs). Start with the front door:

**→ [`docs/WATCHFLOW.md`](./docs/WATCHFLOW.md)** — project overview and the index to
every other document (architecture, constitution, coding standard, security model,
roadmap, and more).

## License

[MIT](./LICENSE) © San Shibu (`ScorpioCodeX`)
